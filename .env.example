# Optional during development; set the real HTTPS origin before deployment.
# SITE_URL=https://your-real-domain.no

# Optionally replace with a direct HTTPS Pages CMS repository link.
PUBLIC_CMS_URL=https://app.pagescms.org

# Keep SFTP credentials in GitHub Actions Secrets; see docs/deployment.md.
